# Three GEMM variants, each a standalone program built with the same flags.
CUFILES := gemm_tiled_double_buffering.cu \
           gemm_tiled_warp_specialization.cu \
           gemm_cooperative_pipeline.cu

include ../common.mk
